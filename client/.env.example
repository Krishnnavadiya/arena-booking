# Copy to ".env" for local overrides (optional).
# Leave empty in local dev so Vite proxies /api to the local server.
# In production (Vercel), set this to your Render backend URL, e.g.
# VITE_API_URL=https://arena-api.onrender.com
VITE_API_URL=
