// Domain shapes (documented for reference; engine is plain JS):
// Court            { id, name, type: 'small'|'large', format, description }
// CourtStatus      'available' | 'booked' | 'blocked'
// AvailabilityResult { booked: string[], blocked: string[], available: string[] }

export {};
